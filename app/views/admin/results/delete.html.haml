- content_for :sidebar do
  = render :partial => 'admin/sidebar'

- if notice.present?
  %p#notice.alert.alert-success= notice

%h1
  %i.icon-remove
  Deleting result for #{@event.name}

.row-fluid
  .section.span12
    %h4.title= icon_title_question 'Confirmation:'

    %div
      %p Are you sure you want to delete result for #{@event.name}</strong>?
      %p Doing so will trigger the following actions:
      %ul
        - case @result.type
        - when 'TeamResult'
          %li Remove this result from the #{link_to @result.participant.chapter, chapter_team_path(@result.participant.chapter, @result.participant)} team
        - else
          %li Remove this result from #{link_to @result.participant, chapter_student_path(@result.participant.chapter, @result.participant)}

    %div
      = link_to icon_title_delete,  admin_event_result_path(@event, @result), :method => :delete, :class => 'btn btn-small btn-danger'
      = link_to icon_title('arrow-left', 'Cancel'), :back, :class => 'btn btn-small'