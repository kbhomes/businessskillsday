- title "New result for #{@event.name}"

- content_for :sidebar do
  = render :partial => 'admin/sidebar'

.row-fluid
  .span12
    %h1= icon_title_new "Adding a new result for #{link_to event_with_icon(@event), admin_event_path(@event)}"

.row-fluid
  .span12
    = render :partial => 'form'